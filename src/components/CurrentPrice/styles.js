import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
  headerPrice: {
    width: '100%',
    height: 'auto',
    alignItems: 'center',
  },
  currentPrice: {
    fontSize: 32,
    fontWeight: 'bold',
    color: '#F6921E',
    marginTop: 20,
  },
  textPrice: {
    fontSize: 16,
    fontWeight: 'bold',
    color: '#FFFFFF',
  },
})

export default styles